[package]
name = "ctap"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "The CTAPHID transport and the CTAP2 request parsers, once, testable without a board"

[dependencies]
cbor = { path = "../cbor" }

[workspace]
